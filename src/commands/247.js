const {SlashCommandBuilder}=require('discord.js');const {player,same,persistent}=require('./_helpers');const {actionCard}=require('../ui/embeds');module.exports={data:new SlashCommandBuilder().setName('247').setDescription('Keep the bot in voice 24/7').addBooleanOption(o=>o.setName('enabled').setDescription('Enable 24/7').setRequired(true)),async execute(i){const p=player(i);const ch=same(i,p);p.always247=i.options.getBoolean('enabled',true);if(p.always247)await p.connect(ch);else {try{p.connection?.destroy()}catch{} p.connection=null;p.voiceChannelId=null;p.channel=null;}await persistent(i,actionCard('♾️','24/7 Updated',`24/7 is now **${p.always247?'ON':'OFF'}** by <@${i.user.id}>.`));}};