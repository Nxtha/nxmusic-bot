const {SlashCommandBuilder}=require('discord.js');const {player,same,temp}=require('./_helpers');const {actionCard}=require('../ui/embeds');module.exports={data:new SlashCommandBuilder().setName('remove').setDescription('Remove a queued track').addIntegerOption(o=>o.setName('position').setDescription('Queue position').setRequired(true).setMinValue(1)),async execute(i){const p=player(i);same(i,p);const x=p.remove(i.options.getInteger('position',true));if(!x)throw new Error('NO_RESULTS');await temp(i,actionCard('🗑️','Track Removed',`Removed **${x.title}** by <@${i.user.id}>.`));}};