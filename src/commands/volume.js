const {SlashCommandBuilder}=require('discord.js');const {player,same,temp}=require('./_helpers');const {actionCard}=require('../ui/embeds');module.exports={data:new SlashCommandBuilder().setName('volume').setDescription('Set volume').addIntegerOption(o=>o.setName('value').setDescription('0-100').setRequired(true).setMinValue(0).setMaxValue(100)),async execute(i){const p=player(i);same(i,p);p.setVolume(i.options.getInteger('value',true));await temp(i,actionCard('🔊','Volume Changed',`Volume set to **${p.volume}%** by <@${i.user.id}>.`));}};