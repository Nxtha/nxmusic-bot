const {Client,GatewayIntentBits,Partials,REST,Routes}=require('discord.js');const config=require('./config/config');const {commands}=require('./commands');const interaction=require('./events/interactionCreate');const ready=require('./events/ready');const voiceState=require('./events/voiceStateUpdate');if(!config.token)throw new Error('Missing DISCORD_TOKEN in .env');
const client=new Client({intents:[GatewayIntentBits.Guilds,GatewayIntentBits.GuildVoiceStates],partials:[Partials.Channel]});
client.once('ready',async c=>{ready(c);const rest=new REST({version:'10'}).setToken(config.token);try{const body=commands.map(x=>x.data.toJSON());if(config.guildId)await rest.put(Routes.applicationGuildCommands(config.clientId,config.guildId),{body});else await rest.put(Routes.applicationCommands(config.clientId),{body});console.log(`[commands] registered ${body.length} commands.`)}catch(e){console.error('[commands] registration failed:',e.message)}});
client.on('interactionCreate',interaction);client.on('voiceStateUpdate',voiceState);client.login(config.token);module.exports=client;
